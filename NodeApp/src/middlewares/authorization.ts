import {Request,Response, NextFunction } from "express"

// validate token and see if it is up to date
const Authorization = (req: Request, res: Response,next:NextFunction) => {
 
}

export {Authorization}